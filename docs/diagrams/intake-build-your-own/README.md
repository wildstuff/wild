# Figures — build your own intake flow

Structural diagrams for [`../../intake-build-your-own.md`](../../intake-build-your-own.md).
Each is embedded inline in the doc (GitHub renders the Mermaid); the
`.mmd` sources here are kept tunable. Edit a source, run `./render.sh`,
the `rendered/` SVG + PNG regenerate.

| Source | Section in the doc |
|---|---|
| `mermaid/01-building-blocks.mmd` | The one idea — what you build vs. what's fixed |
| `mermaid/02-four-steps.mmd` | The four steps (with the self-extending loop) |
| `mermaid/03-runner-per-file.mmd` | Step 4 — the Runner's per-file decision |
| `mermaid/04-the-switches.mmd` | The two switches — routing & mapping |
