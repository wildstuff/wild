# Figures for `cli.md`

Tunable diagram sources for [`../../cli.md`](../../cli.md). Edit a
`.mmd`, run `./render.sh`, the exports in `rendered/` regenerate. See
[`../how-tribes-live/README.md`](../how-tribes-live/README.md) for the
pattern and [`../../STYLE.md`](../../STYLE.md) for the house style.

| Figure | Section |
|---|---|
| `mermaid/01-surfaces.mmd` | one binary, three surfaces |
| `mermaid/02-command-families.mmd` | command families at a glance |

The doc embeds these Mermaid blocks inline (GitHub renders them);
`rendered/` is for slide decks and other non-GitHub contexts.
