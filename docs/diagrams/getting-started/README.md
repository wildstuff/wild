# Figures for `getting-started.md`

Tunable diagram sources for [`../../getting-started.md`](../../getting-started.md).
Edit a `.mmd`, run `./render.sh`, the exports in `rendered/` regenerate.
See [`../how-tribes-live/README.md`](../how-tribes-live/README.md) for the
full pattern and [`../../STYLE.md`](../../STYLE.md) for the house style.

| Figure | Section |
|---|---|
| `mermaid/01-overview.mmd` | the five-step overview strip |
| `mermaid/02-boot-sequence.mmd` | what `wild up` does on first boot |

The doc embeds these Mermaid blocks inline (GitHub renders them);
`rendered/` is for slide decks and other non-GitHub contexts.
